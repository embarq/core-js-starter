# Simple starter template for VanillaJS with live-reload

- clone this repo: `$ git clone https://github.com/embarq/core-js-starter.git`
- install dependencies: `$ npm install`
- run with: `$ npm start`

And here you are!
